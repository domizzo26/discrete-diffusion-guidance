# @package _global_
data:
  train: ${env:DATASET_PATH}
  valid: ${env:DATASET_PATH}
  label_col: labels
  num_classes: 10
  streaming: False
  size: 1024
  length: 3072
  add_special_tokens: True
  add_mask_token: True
  tokenizer_name_or_path: raw_pixels